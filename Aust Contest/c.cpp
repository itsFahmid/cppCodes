#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int q, k, l, r;
        cin >> q >> k >> l >> r;
        for(int i=0; i<l; i++){
            if(pow(i, k) < r && )
        }
    }
}

